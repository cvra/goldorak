sudo: required

services:
    - docker

before_install:
    - git submodule update --init --recursive
    - docker pull cvra/goldorak
    - docker run -it -v $(pwd)/:/home/ubuntu/catkin_ws/src/goldorak cvra/goldorak /bin/bash -c "cd /home/ubuntu/catkin_ws/src/goldorak"

script:
    - ./build.sh
